import { GreenButtonDirective } from './green-button.directive';

describe('GreenButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenButtonDirective();
    expect(directive).toBeTruthy();
  });
});
